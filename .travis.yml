language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - hhvm

install: travis_retry composer install --no-interaction --prefer-source

matrix:
  allow_failures:
    - php: hhvm
  fast_finish: true
