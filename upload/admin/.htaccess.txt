## SEO Friendly INDEX (Single Store)
## Use RewriteRule ^index\.php$ http://%{HTTP_HOST}/folder_name? [R=301,L] if your website is located within a folder inside the web root.

# RewriteCond %{QUERY_STRING} ^route=common/home$
# RewriteCond %{REQUEST_METHOD} !^POST$
# RewriteRule ^index\.php$ http://%{HTTP_HOST}? [R=301,L]
