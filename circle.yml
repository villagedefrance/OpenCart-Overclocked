machine:
  timezone:
    Europe/Dublin
  php:
    version: 7.0.4
  environment:
    CIRCLE_ENV: test

test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/phpunit
    - phpunit --log-junit $CIRCLE_TEST_REPORTS/phpunit/junit.xml tests

deployment:
  staging:
    branch: master
